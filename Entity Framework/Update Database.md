# Update Database

```bash
dotnet ef database update --project Immovlan.SqlServer --context ImmovlanContext --connection "Server=.;Database=Immo;Trusted_Connection=True;"
```

```bash
dotnet ef database update --project Immovlan.SqlServer --context ImmovlanContext --connection "Server=xxx;Database=xxx;User Id=xxx;Password=xxx;TrustServerCertificate=true;"
```
